[phases.setup]
nixPkgs = ["ffmpeg", "python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn app:app --workers 1 --threads 8 --timeout 600 --bind 0.0.0.0:$PORT"
